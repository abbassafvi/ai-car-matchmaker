pytest>=9,<10

# MCP server (T023). Pinned <2 deliberately: agent-backend consumes these
# servers through langchain-mcp-adapters 0.3.x, which requires mcp<2.0.0.
# mcp 2.0.0 exists on PyPI, so an unpinned install silently splits the two
# sides of the protocol across a major version.
mcp>=1.24,<2
uvicorn[standard]>=0.52,<0.53
